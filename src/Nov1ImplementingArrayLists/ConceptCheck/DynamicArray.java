package Nov1ImplementingArrayLists.ConceptCheck;

public class DynamicArray<E>{
    // Instance Variables
    private final int DEFAULT_SIZE = 10;
    private E[] array;
    private int numberOfElements;

    // Constructors
    public DynamicArray() {
        array = (E[]) new Object[DEFAULT_SIZE];
        numberOfElements = 0;
    }

    // Remove element from an index


    // Instance Methods
}
